
1. git status
  da provjerimo koje smo stvari mijenjali
  
2. git add -A  ili git add --all
  da dodamo te stvari na stage

3. git commit -m "message"
  dodamo te promjene u commit
  
4. git push -u origin master
  pushamo te stvari na git
